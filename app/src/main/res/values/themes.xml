<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema principal de la app -->
    <style name="Theme.Taller1" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Barra de estado -->
        <item name="android:statusBarColor">@color/purpleTopBar</item>
        <!-- Barra de navegación -->
        <item name="android:navigationBarColor">@color/bgDark</item>
    </style>

</resources>