<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.taller03092025">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Taller1">

        <activity android:name=".MenuActivity" />

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- (opcional)
        <activity android:name=".MainActivity" />
        -->

        <!-- Agregarás luego:
        <activity android:name=".CalculadoraActivity" />
        <activity android:name=".PromedioActivity" />
        <activity android:name=".ConversorActivity" />
        <activity android:name=".PropinasActivity" />
        -->

    </application>
</manifest>
